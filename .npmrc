# Publishing and login must hit the official npm registry, not the
# npmmirror mirror configured in the user-level .npmrc (the mirror has its
# own account system and cannot host @lemcae publishes). Project-level
# config wins over user-level, so this overrides the mirror for this repo
# only; dependency installs in other repositories keep their mirror speed.
registry=https://registry.npmjs.org/
